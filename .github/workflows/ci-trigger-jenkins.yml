name: Java 21 CI and Jenkins Deployment Trigger

on:
  push:
    branches:
      - main
      - webapp

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build application
        run: mvn clean package

      - name: Trigger Jenkins (main only)
        if: github.ref == 'refs/heads/main'
        run: |
          curl -X POST           "${{ secrets.JENKINS_URL }}/job/insured-assurance-deploy/build"           --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}"
